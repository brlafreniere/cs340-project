<%- contentFor('content') %>

<div class="border p-3 mt-3">
  <form method="POST" action="/pilots/<%= pilot.pilot_id %>/delete">
    <legend><strong>Delete Pilot</strong></legend>

    <div>
      <p>Are you sure you wish to delete the following?</p>
      <table class="table border">
        <thead>
          <tr>
            <th>Pilot ID</th>
            <th>First Name</th>
            <th>Last Name</th>
          </tr>
        </thead>
        <tr>
          <td><%= pilot.pilot_id %></td>
          <td><%= pilot.first_name %></td>
          <td><%= pilot.last_name %></td>
        </tr>
      </table>
    </div>

    <div class="mt-3">
      <input class="btn btn-danger" type="submit" value="Delete Pilot">
      <input class="btn btn-secondary" type="button" value="Cancel" onClick="javascript:history.back()">
    </div>
  </form>
</div>